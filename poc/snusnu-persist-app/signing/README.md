# Development signing identity

`snusnu-debug.keystore` is intentionally public and is used only to keep local
rebuilds upgrade-compatible with the checked-in persistence APK. It provides
application identity continuity, not secrecy or production trust.
